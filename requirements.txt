# Networking & Machine Learning Math
paho-mqtt>=2.1.0
numpy>=1.24.0

# Protocol & ML Core Utilities
scikit-learn>=1.0.0
pandas>=2.0
scipy>=1.11                # required by scikit-learn internally

# Core Cryptography & SSS (Stacks & Utils)
cryptography>=41.0.0
pycryptodome>=3.23.0

# Lightweight PRG Block Ciphers (Mask Expansion)
simonspeckciphers>=1.0.0

# Metrics — CPU / RAM sampling (metrics/metrics_collector.py)
psutil>=5.9.0

# Kaggle dataset auto-download (used by fl_core/model.py)
kagglehub>=0.3.0


